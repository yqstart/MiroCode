# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号遵循语义化版本。

## [Unreleased]

### 修复

- 修复打开文件后「标签已出现但编辑区黑屏/空白、必须重启」的问题：画布视图切换不再依赖 out-in 过渡与 CSS transition（其进入/离场动画依赖双 rAF 与动画事件，WKWebView 长时间空闲或被遮挡后 rAF 可能被丢弃，旧视图永久卡在离场、新视图永不挂载）；改为同时过渡 + 绝对定位叠放（新视图在上层、离场视图在下层并禁用交互），动画改用 CSS animation（事件丢失时元素回到自身样式保持可见）并加 :duration 兜底清理残留过渡类。大文件打开与长时间空闲后打开文件的场景均不再黑屏。
- 编辑器在窗口恢复焦点 / 页面重新可见时强制重新测量，防止 WKWebView 空闲恢复绘制后 CodeMirror 视图停留空白；测量请求 rAF 之外增加 setTimeout 兜底（恢复期 rAF 可能被丢弃）。
- 修复资源树右键「在终端中打开」失效：此前仅展开终端面板，已有终端时不会进入目标目录。现在文件取其父目录、文件夹取其自身，面板展开后活动终端已在该目录且空闲则复用，否则新开终端并以目标目录为工作目录。
- 修复 scripts 快捷方式注入命令时对普通键名多余加引号（`npm run dev` 显示为 `npm run 'dev'`）：仅由字母数字与 `: . _ @ / -` 组成的键名原样拼接，含空格或 shell 特殊字符的键名仍单引号转义（保留注入防护）。
- 终端标签命名对齐 Cursor：默认显示 shell 名（zsh / bash / powershell），同一 shell 多个终端按 `zsh (2)` 加序号并随关闭自动重排；不再把最近输入的命令作为标签标题。
- 修复 Markdown 预览无法选中文本：全局 `user-select: none`（应用型 UI 防误选）的恢复清单未覆盖预览内容区，预览现已可选中并 ⌘/Ctrl+C 复制。

## [0.1.5] - 2026-08-25

### 修复

- 修复编辑器标签与编辑内容错位：并发打开同一文件产生重复标签、异步读盘完成抢占当前标签、输入后立即切换标签把光标写进错误标签。
- 修复 Git 合并、拉取与更新操作的多个问题：分支名含斜杠（如 `feature/foo`）被误截断导致合并找不到分支；工作树有未提交改动时 libgit2 静默覆盖本地修改（快进与真实合并路径）；合并冲突解决后提交丢失双亲导致合并拓扑丢失、再次合并凭空冲突。
- 修复 Git 状态缓存并发刷新：暂存/提交前已启动的旧刷新请求不得在操作完成后重新写入旧快照，操作后状态立即可见。

### 新增

- 合并冲突时支持从 Commit 面板「放弃合并」，恢复工作树并清除合并中间状态。

## [0.1.4] - 2026-08-25

### 修复

- 修复 Git 分支查询、工作区目录展开与快捷定位失败时的错误处理，保持状态刷新和导航可用。
- 修复搜索超时定时器未清理、终端 shell 退出后 PTY 状态残留、远程终端隐藏保活时重复调整尺寸等问题。
- 修复 Markdown 代码高亮 token 损坏、工作区切换后旧符号索引回写，以及 Git Graph 父提交连线刷新竞态。
- 修复 macOS 安全书签并发访问和标题栏 AppKit 调用线程问题。

### 改进

- 优化设置持久化、编辑器状态栏和 Git 高频刷新，降低输入与状态更新开销。
- 优化终端忙闲检测和命令输出缓冲，减少任务误判与无界内存增长。
- 为大文件替换增加 2MB 护栏，并在差异计算中优化公共前后缀处理和超大变更降级策略。
- 改进编辑器会话快照配额处理，超限时分级降级而不是整体丢失。

### 安全

- 加强更新说明 Markdown 链接过滤，覆盖实体、百分号编码和控制字符。
- 修复终端脚本名称 shell 转义，避免 `package.json` 脚本键名触发命令注入。

## [0.1.3] - 2026-08-24

### 新增

- 新增编辑器 F8 / Shift+F8 诊断导航，以及 `⌘K ⌘F`（Windows/Linux 为 `Ctrl+K Ctrl+F`）单一选区格式化。
- 新增按工作区隔离的自定义终端指令，可与 `package.json` scripts 一起运行并固定到终端顶栏。

### 改进

- 收敛编辑器应用命令与 CodeMirror 原生 keymap 的优先级，保留多光标、行操作、注释、折叠、查找选择等高频能力；跳转无目标时回退到原生编辑行为。
- 选区格式化支持项目 Prettier 与内置 Prettier，选区外内容保持不变，并作为一次历史操作提交以支持立即撤销。
- 扩充设置页与使用说明中的编辑器快捷键清单，修复 `⌘K` 提交面板与选区格式化连续快捷键的冲突。
- 终端标签显示最近执行的命令并限制过长标题；优化窄侧栏提交操作、侧栏拖拽和 macOS 新窗口首次点击体验。

## [0.1.2] - 2026-08-24

### 修复

- 修复在大量文件变更、且文件所在父目录已被删除时执行全部回滚会报 no such file or directory (os error 2) 的问题。

### 改进

- 改进多窗口工作区、编辑器标签和本地终端会话的退出保存与启动恢复，并按窗口隔离会话状态。
- 改进 macOS 工作区安全书签的恢复与保存时序，减少重复授权和启动恢复竞态。

## [0.1.1] - 2026-08-21

### 修复

- 修复编辑器在窗口恢复、视图切换或终端面板布局变化后首次测量高度为 0，导致编辑区空白的问题。
- 修复终端高频输出阻塞 WebView 渲染的问题，增加有界输出队列并限制终端回滚区大小。
- 修复外部程序持续写入文件时自动保存反复覆盖本地编辑内容的问题，并降低文件监听刷新频率。
- 修复 macOS Dock 菜单在非主线程调用 AppKit 导致菜单更新失败的问题。

### 改进

- 优化 Git 提交区的提交、提交并推送及贮藏操作布局，改进行级操作按钮和差异视图的主题适配。
- 调整比较视图标签与 Markdown 预览/编辑切换控件的位置，改善编辑区布局。
- 移除设置页中独立的代码补全说明卡片，保留编辑器补全功能及快捷键。
- 优化打开文件夹按钮的悬浮反馈，移除位置跳动并简化按钮文案。

## [0.1.0] - 2026-08-20

### 首个可用版本

- 当前代码重新作为 Miro Code 的首个可用版本发布，覆盖资源树、代码编辑、搜索、Git、终端与 SSH、主题、会话恢复等核心工作流。
- 提供离线代码补全、JavaScript / TypeScript 类型服务、Vue 单文件组件辅助，以及内置格式化能力。
- 保持轻量、跨平台和离线优先，不包含联网 AI 补全、对话面板或 Agent 能力。
- 移除编辑器 Minimap 及其设置项、Canvas 绘制和点击定位逻辑。

[0.1.5]: https://github.com/yqstart/MiroCode/releases/tag/v0.1.5
[0.1.4]: https://github.com/yqstart/MiroCode/releases/tag/v0.1.4
[0.1.3]: https://github.com/yqstart/MiroCode/releases/tag/v0.1.3
[0.1.2]: https://github.com/yqstart/MiroCode/releases/tag/v0.1.2
[0.1.1]: https://github.com/yqstart/MiroCode/releases/tag/v0.1.1
[0.1.0]: https://github.com/yqstart/MiroCode/releases/tag/v0.1.0
