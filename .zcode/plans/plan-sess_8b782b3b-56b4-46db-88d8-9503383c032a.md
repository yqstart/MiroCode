## 第一期收尾实施方案（Tier A 共 4 项 + Tailwind `inClassAttr` 正则扩）

按你的选择，只做最小收尾：4 项体验/i18n 修补 + 1 项正则扩，全部纯前端改动，不动 Rust / 架构 / CHANGELOG 版本号。

### 范围

| 编号 | 项目 | 关键文件 | 改动 |
|---|---|---|---|
| A-1 | `ImagePreview.vue` 7 处硬编码中文 → i18n | `src/features/editor/ImagePreview.vue:93,102,118,137,157,163,167,176,185` + `src/i18n/locales/{zh-CN,en-US}.ts` | 新增 7 个 `editor.image.*` key（zh/en），`useI18n` 替换 7 处硬编码 |
| A-2 | 资源树 "在终端中打开" 菜单项 | `src/features/explorer/ExplorerPanel.vue:800-861, 470-552` | i18n key `explorer.openInTerminal` 已留（zh-CN:174 / en-US:177），菜单 DOM 加项 + `runMenu('open-in-terminal')` + 调 `sessions.openSessions`（实施前 read `stores/sessions.ts` 确认签名） |
| A-3 | 资源树 "复制文件名" 菜单项 | `src/features/explorer/ExplorerPanel.vue` | 新增 zh/en `explorer.copyFileName` / `explorer.copiedFileName` + 菜单 DOM + `runMenu('copy-file-name')` + `clipboard.writeText(basename(target.relPath))` |
| A-4 | 窗口级快捷键补齐 | `src/app/AppShell.vue:107-169` | `onKeydown` 增加：`⌘W` → `editor.closeTab(activePath)`；`⌘⌥→` / `⌘⌥←` → 切标签（如 store 无对应 action 则新增 `activateNextTab/activatePrevTab`）；`⌘R` → `workspace.refreshFromDisk()`。守卫沿用现有 `isEditableTarget` |
| C-1 | Tailwind `inClassAttr` 正则扩 | `src/features/editor/completions.ts:424` | 当前 `/class\s*=\s*["'][^"']*$/` → `/(?:class|className|:class|class:list)\s*=\s*["'{][^"'{\]]*$/`，让 `className=`（React/TSX）/ `:class`（Vue）/ `class:list=`（Svelte）也能触发补全 |

### 不做（明确排除）

- **Tier B/C/D 其余全部**：`window.confirm` 统一替换、CommitPanel ⋯ 菜单、状态栏最近项目、查找面板 Alt+Enter、资源树 F2/Enter/Delete、Tailwind config 解析、更新流程取消/重试
- **任何架构级变更**（目录结构、模块边界、技术选型）——按 `AGENTS.md` 约定，**不**回写 `AGENTS.md` / `docs/Miro Code技术架构文档.md`
- **Rust 端**（不动 `src-tauri/`）
- **命令面板、分屏、工具栏自定义、LSP、插件体系**（文档已声明"一期不做"）
- **CHANGELOG 版本号**（仅在 `[Unreleased]` 追加条目，下次发版时归入）

### 实施顺序

1. **A-1**（独立，最小）→ `pnpm build` 验证
2. **A-2 / A-3**（资源树菜单小改）→ i18n 键同时写齐
3. **A-4**（快捷键）→ 冒烟 4 个键的焦点守卫
4. **C-1**（一行正则）→ `.tsx` 与 `.vue` 双向冒烟
5. **全局**：`pnpm build` + `cargo check --manifest-path src-tauri/Cargo.toml` 双绿；手动冒烟；写 CHANGELOG `[Unreleased]` 条目

### 验证清单

| 项 | 标准 |
|---|---|
| A-1 | 切换 zh-CN/en-US 后 7 处文案跟随；类型检查通过 |
| A-2 | 右键目录/文件 → 在终端中打开 → 活动栏出现会话标签，cwd 正确 |
| A-3 | 右键文件 → 复制文件名 → 剪贴板仅含文件名（如 `index.vue`），无路径 |
| A-4 | 编辑器焦点下 ⌘W 关标签、⌘⌥→← 切标签、⌘R 刷新树；QuickOpen/find panel/终端内不响应 |
| C-1 | `.tsx` `className="fl"` 命中 `flex`；`.vue` `:class="fl"` 命中；不破坏现有 `.vue` `class=""` 行为 |
| 全局 | `pnpm build` + `cargo check` 双绿；无新增 lint 错误 |

### CHANGELOG

仅在 `CHANGELOG.md` 的 `[Unreleased]` 段下追加 5 条 bullet（不归入版本号、不打 tag），下次发版时统一归入新版本节。条目内容草稿：

- **修复（i18n）**：`ImagePreview.vue` 7 处硬编码中文接入 i18n（新增 `editor.image.*` 键）
- **新增**：资源树右键菜单新增"在终端中打开"
- **新增**：资源树右键菜单新增"复制文件名"
- **新增**：窗口级快捷键补齐（`⌘W` 关闭标签 / `⌘⌥→` `⌘⌥←` 切标签 / `⌘R` 刷新树）
- **修复（补全）**：Tailwind 类名补全在 `className=` / `:class` / `class:list=` 触发条件下生效

### 风险点

| 风险 | 应对 |
|---|---|
| `sessions.openSessions` API 签名 | 实施前 read `stores/sessions.ts` 确认；不匹配则用最接近的等价调用 |
| 资源树右键菜单 11 → 12 项挤 | 现有设计已留 `openInTerminal` key，说明位置已规划 |
| `clipboard.writeText` 走 Tauri 还是浏览器 | 沿用 `shared/clipboard.ts` 现有封装 |
| `⌘W` 与 macOS 关闭窗冲突 | WebView 内 `keydown` 监听优先级通常更高；冒烟验证；如冲突再加 macOS 平台判断 |
| 快捷键在 QuickOpen/find panel 内误触发 | 沿用现有 `isEditableTarget` 守卫 + `e.target` 检查 |

### 关键前置 read（实施第一步必做）

1. `src/stores/sessions.ts` — 确认 `openSessions` 签名（A-2）
2. `src/stores/editor.ts` — 确认 `closeTab` / 切标签 action 存在性（A-4）
3. `src/stores/workspace.ts` — 确认 `refreshFromDisk` action（A-4）
4. `src/shared/clipboard.ts` — 确认 API（A-3）
5. `src/features/editor/completions.ts:415-444` — 重读 `tailwindSource` 上下文（C-1）